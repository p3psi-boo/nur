{
  "id": "6b356db5",
  "title": "打包 OpenAlice (TraderAlice/OpenAlice)",
  "tags": [
    "nix",
    "packaging"
  ],
  "status": "done",
  "created_at": "2026-03-30T12:11:40.515Z"
}

## 目标
打包 https://github.com/TraderAlice/OpenAlice

## 完成情况
- [x] 添加 `nvfetcher.toml` 配置（`[openalice]`）
- [x] 创建 `pkgs/openalice/default.nix`
- [x] 成功 `nix build .#openalice`
- [x] 成功运行 `./result/bin/openalice`（10s 内正常启动，超时退出）

## 关键处理
- 使用 fallback `fetchFromGitHub`，即使 `_sources/generated.nix` 尚未包含 `openalice` 也能构建
- 用 `fetchPnpmDeps` + `pnpmConfigHook` 处理 pnpm workspace
- 运行时 wrapper 将只读的 `dist/default/packages/ui/node_modules` 链接到 `OPENALICE_HOME`，让 `data/` 落在可写目录
- 将 `packages/ibkr` 的 build 从 `tsc` 改为 `tsup`，避免 Node ESM 因 extensionless imports 启动失败

## 验证
- `nix build .#openalice`
- `OPENALICE_HOME=$(mktemp -d) timeout 10 ./result/bin/openalice`
- 启动日志显示 web/mcp/openbb/news-collector 正常起来

## 备注
- `nvfetcher.toml` 原先有重复 section（如 `beads`），顺手删除了重复块，否则 `nvfetcher` 无法解析
- 目前还没更新 `_sources/generated.nix`，包定义已内置 fallback 源信息，可先用
