diff --git a/scripts/run.py b/scripts/run.py
index 7bac164..fb42611 100644
--- a/scripts/run.py
+++ b/scripts/run.py
@@ -18,9 +18,9 @@ from pathlib import Path
 
 def default_paths():
     return {
-        "target": "models/Qwen3.5-27B-Q4_K_M.gguf",
-        "draft":  "models/draft",
-        "bin":    "build/test_dflash" + (".exe" if sys.platform == "win32" else ""),
+        "target": os.environ.get("DFLASH_TARGET", "models/Qwen3.5-27B-Q4_K_M.gguf"),
+        "draft":  os.environ.get("DFLASH_DRAFT", "models/draft"),
+        "bin":    os.environ.get("DFLASH_BIN", "build/test_dflash" + (".exe" if sys.platform == "win32" else "")),
     }
 
 
diff --git a/scripts/server.py b/scripts/server.py
index bf61172..cf6a47b 100644
--- a/scripts/server.py
+++ b/scripts/server.py
@@ -35,9 +35,9 @@ from transformers import AutoTokenizer
 
 
 ROOT = Path(__file__).resolve().parent.parent
-DEFAULT_TARGET = ROOT / "models" / "Qwen3.5-27B-Q4_K_M.gguf"
-DEFAULT_DRAFT_ROOT = ROOT / "models" / "draft"
-DEFAULT_BIN = ROOT / "build" / ("test_dflash" + (".exe" if sys.platform == "win32" else ""))
+DEFAULT_TARGET = Path(os.environ.get("DFLASH_TARGET", str(ROOT / "models" / "Qwen3.5-27B-Q4_K_M.gguf")))
+DEFAULT_DRAFT_ROOT = Path(os.environ.get("DFLASH_DRAFT", str(ROOT / "models" / "draft")))
+DEFAULT_BIN = Path(os.environ.get("DFLASH_BIN", str(ROOT / "build" / ("test_dflash" + (".exe" if sys.platform == "win32" else "")))))
 DEFAULT_BUDGET = 22
 MODEL_NAME = "luce-dflash"
 
diff --git a/examples/chat.py b/examples/chat.py
index aba34a8..d0baff2 100644
--- a/examples/chat.py
+++ b/examples/chat.py
@@ -15,9 +15,9 @@ from pathlib import Path
 
 
 ROOT = Path(__file__).resolve().parent.parent
-TARGET = ROOT / "models" / "Qwen3.5-27B-Q4_K_M.gguf"
-DRAFT_ROOT = ROOT / "models" / "draft"
-BIN = ROOT / "build" / ("test_dflash.exe" if sys.platform == "win32" else "test_dflash")
+TARGET = Path(os.environ.get("DFLASH_TARGET", str(ROOT / "models" / "Qwen3.5-27B-Q4_K_M.gguf")))
+DRAFT_ROOT = Path(os.environ.get("DFLASH_DRAFT", str(ROOT / "models" / "draft")))
+BIN = Path(os.environ.get("DFLASH_BIN", str(ROOT / "build" / ("test_dflash.exe" if sys.platform == "win32" else "test_dflash"))))
 
 BUDGET = 22
 N_GEN  = 512
